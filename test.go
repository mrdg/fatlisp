package main

import(
    "fmt"
    "strings"
)

func main() {
    // a := make([]interface{}, 2)
    // b := make([]interface{}, 1)
    // a = append(a, b)

    // c := a[2].([]interface{})

    // fmt.Println(c[0])
    fmt.Println(strings.Fields(" ( + 1 1 ) "))

}
